# config.js tracked intentionally — private repo only

# Local noise — never belongs in the repo
.DS_Store
.playwright-mcp/
.wrangler/
.remember/

# Apps Script source is private (origin only)
google-apps-script/
